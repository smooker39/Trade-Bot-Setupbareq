## Packages
recharts | Data visualization for price charts and balance history
date-fns | Formatting timestamps for logs and history tables
framer-motion | Smooth transitions for status indicators and list updates
clsx | Utility for conditional class names (often used with tailwind-merge)
tailwind-merge | Utility for merging tailwind classes

## Notes
Polling interval should be 2-5 seconds for /api/health to update status bar
Theme should be strictly dark mode with neon accents (green/red)
Recharts will use mock data initially if historical data isn't fully provided by backend API yet
